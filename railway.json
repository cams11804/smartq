{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:$PORT",
    "healthcheckPath": "/index.php"
  }
}
```
Press **Ctrl + S**

---

## Step 4 — Push to GitHub
```
git add .
```
```
git commit -m "Simplify Railway config"
```
```
git push origin main
```

---

## Step 5 — Also Set the PORT Variable in Railway

1. Go to Railway → click **smartq** box → **Variables** tab
2. Check if `PORT` variable exists — if yes **delete it**
3. Railway sets PORT automatically — we don't need to manually set it

---

## Step 6 — Wait for Redeploy

Go to **Deployments** tab and wait for ✅ **Active**

Then test:
```
https://smartq-production-0518.up.railway.app/index.php